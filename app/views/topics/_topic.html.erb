<li class="topic tag" id="topic-<%= topic.id %>">
	<%= truncate(topic.name, length: 18) %>
	<%= link_to topic_path(topic), method: :delete do %>
		<i class="fa fa-close pull-right"></i>
	<% end %>
</li>